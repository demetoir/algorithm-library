uses dorand;
var i:integer;
const minlongint=-maxlongint-1;
begin
  regint (92);
  writeln (500);
  for i:=1 to 500 do writeln (lr (1, 2)-maxlongint-2);
  writeln (500);
  for i:=1 to 500 do writeln (lr (1, 2)-maxlongint-2);
end.