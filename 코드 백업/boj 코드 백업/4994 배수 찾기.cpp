#include<stdio.h>

#define si(a) scanf("%d",&(a));
#define MAX_N 20000
#define LL long long

int n,m;

char ans[100];

int main() {
	while (1) {
		si(n);
		if (n == 0) break;

		printf("%d\n", ans);
	}
	return 0;
}